SELECT 
    operation_type,
    table_name,
    CASE 
        WHEN operation_type = 'INSERT' THEN operation_code
        ELSE changed_column
    END AS changes_info
FROM your_table_name
ORDER BY log_timestamp, log_id;
