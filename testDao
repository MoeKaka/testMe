package com.example;

import static org.mockito.Mockito.*;
import static org.junit.Assert.*;

import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.Mock;
import org.mockito.junit.MockitoJUnitRunner;

import javax.persistence.EntityManager;
import javax.persistence.Query;
import java.util.Date;
import java.util.stream.Stream;
import java.util.Arrays;

@RunWith(MockitoJUnitRunner.class)
public class MyQueryJpaDaoTest {

    @Mock
    private EntityManager entityManager;

    @Mock
    private Query query;

    private MyQueryJpaDao dao;

    @Before
    public void setUp() {
        dao = new MyQueryJpaDao() {
            @Override
            protected EntityManager getEntityManager() {
                return entityManager;
            }
        };
        
        when(entityManager.createNativeQuery(anyString())).thenReturn(query);
        when(query.setParameter(anyString(), any())).thenReturn(query);
    }

    @Test
    public void testSimpleExecuteQueries() {
        // Create a Stream of type Object[]
        Stream<Object[]> streamOfArrays = Arrays.asList(
            new Object[]{"TestQuelle", "TestName", "TestRepR", new Date(), new Date(), new Date(), new Date()}
        ).stream();
        
        when(query.getResultStream()).thenReturn(streamOfArrays);

        QueriesResults results = dao.executeQueries(1);

        assertNotNull(results);
    }
}
